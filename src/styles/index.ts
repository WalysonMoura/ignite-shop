import { createStitches } from "@stitches/react";

export const {
  config,
  styled,
  theme,
  css,
  globalCss,
  keyframes,
  getCssText,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      primary: "#000000",
      secondary: "#000000",
    },
  },
});
